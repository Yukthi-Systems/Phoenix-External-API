services:
  redis:
    image: redis:latest
    container_name: rust-api-redis

    ports:
      - "127.0.0.1:6379:6379"
    networks:
      - phoenix-external-api-network

    restart: always

networks:
  phoenix-external-api-network:
    external: true

# Do : docker network create phoenix-external-api-network
